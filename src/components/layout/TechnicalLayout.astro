<div class="technical">
  <slot />
</div>

<style>
  .technical {
    max-width: 1280px;
    margin: 0 auto;
    padding-inline: var(--space-margin-tecnico);
  }

  @media (width <= 520px) {
    .technical {
      padding-inline: var(--space-gap-paragraph);
    }
  }
</style>
